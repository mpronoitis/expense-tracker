spring:
  liquibase:
    enabled: true
    change-log: "classpath:changelog/db.changelog-h2.xml" ##use h2 database structure
  datasource:
    url: spring.datasource.url=jdbc:h2:mem:testdb;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
